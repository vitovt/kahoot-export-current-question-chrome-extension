# Kahoot Question Exporter

A Chrome extension that will export the current Kahoot question and answers into a Markdown snippet that is ready to paste.

## Development

Load the `kahoot-export-current-question-chrome-extension` directory as an unpacked extension in Chrome to test changes.
